# NEXUS — Backlog / TODO на будущее

> Что отложено осознанно (рано / отдельные фичи / в составе другого). Обновляется по ходу.
> Дата: 2026-06-14.

## 🔜 Ближайшее (по приоритету)
1. **Ключи:** DeepInfra `INFERENCE_API_KEY` ПРОПИСАН и валиден, но **нужен баланс DeepInfra** (пополнить $5-10 → «Наша модель» заработает). OpenRouter — вставлен в профиле (бесплатная лестница готова). **Lemon Squeezy ОТКЛОНЁН** (KYC, нет РФ). Нужна РФ-платёжка — кандидаты: **Lava.top** (digital/подписки, самозанятый, готовая ссылка — рекомендую), Продамус, ЮKassa (нужен ИП). `checkout` сделан универсальным (плейсхолдер `{email}` или просто ссылка). Webhook авто-активации — per-provider, backlog; пока активация вручную в админке. Скрипт проверки: `nexus-auth-server/verify_keys.py`.
2. **DaData / Контур.Фокус API** — инструмент агента `due-diligence-checker`: ЕГРЮЛ/суды/ФССП структурированным JSON (dadata.ru/api, бесплатный тир). Чинит то, что fetch_url не читает JS-порталы. Сейчас агенты обходят через rusprofile/list-org + web_search.
3. **ОПЛАТА — открытый вопрос (решить):** выбрать и подключить РФ-платёжку для самозанятого. Кандидаты: Lava.top (digital/подписки, чеки в ФНС авто, РФ-карты+СБП), Продамус, ЮKassa-для-самозанятых, Robokassa. Шаги: (1) создать продукт-подписку NEXUS Pro 990₽/мес → взять ССЫЛКУ → вставить в `NEXUS_CHECKOUT_URL` (этого достаточно для ручной схемы); (2) для АВТО-активации — взять API-ключ платёжки + webhook, НО он заработает только после ДЕПЛОЯ auth-сервера на публичный домен (`auth.nexus-agency.ru`) — сейчас локальный, платёжка не достучится. Обработчик webhook подготовить заранее под выбранную платёжку. Пока оплата не подключена — Pro активируется вручную в админке `:8000/admin`. Связано с backlog «Деплой auth-сервера».
4. **Деплой auth-сервера** на публичный домен (Timeweb) — разблокирует webhook оплаты + делает продукт реально доступным клиентам. Egress-relay NL/PL против блокировки Anthropic из РФ (для тех, кто на Claude-ключе).

## 🧠 hermes-agent — память (статус)
- ✅ **A. Память диалогов** (MEMORY.md + USER.md, инъекция, tool remember) — РЕАЛИЗОВАНО 2026-06-14.
- ✅ **B. Авто-профиль личности** юзера (дешёвая модель извлекает факты после чата) — РЕАЛИЗОВАНО 2026-06-14.
- ⏸ **C. Self-improving skills** (агент сам создаёт/правит навыки из опыта) — ОТЛОЖЕНО: дублирует skill engine, делать в его составе. Взять дизайн hermes: формат SKILL.md, лимиты, progressive disclosure, guard против path-traversal.
- ❌ **D. Multi-provider память** (honcho/mem0 SaaS-абстракция) — НЕ берём, over-engineering.

## 🔌 MCP (Model Context Protocol)
- Форк `artem141414/servers` — НЕ брать (устаревшая копия официального без правок).
- MCP-концепция — реализовать **в составе skill engine** (MCP-мост = его естественная часть). Первыми: Git-MCP (Python, без Node), Filesystem-MCP, Playwright-MCP (браузер для OSINT/маркетинга). Риски: Node-рантайм, пул процессов, async-мост, скоупинг доступа.

## 🟡 vibeyard — паттерны и фичи
- **AI Readiness Score** — фича-продукт: взвешенная оценка готовности проекта/брифа + one-click фиксы. Монетизируемо в Pro. Портировать каркас analyzer (readiness/analyzer.ts).
- **Провайдер-абстракция + capabilities** — формализовать наш мульти-LLM слой (capability-флаги → UI условно включает фичи).
- **Resume-handoff паттерн** — передача контекста агенту через файл-ссылку + read-only; внести в `strategy/coordination/handoff-templates.md`.
- **Insight-детекторы** — проактивные алерты оркестратора (раздутый контекст, недоступный инструмент, большой файл).
- **Session-inspector** — timeline tool-calls + разбивка стоимости в web-UI (усиливает оценку качества прогонов).
- **share-crypto** (Web Crypto AES-GCM/PBKDF2) — если делать шеринг live-прогонов в подписке.
- НЕ брать: Electron/PTY/xterm-слой.

## 🏗 Крупные направления (из роадмапа 08)
- **Skill engine** (provider-agnostic, формат Claude Code SKILL.md, sandbox) — после монетизации. Вбирает MCP + hermes-C.
- **Генерация контента** (видео/аудио/UGC) — до выручки отложено; текст+картинки есть.
- **Соцсети-постинг** (TG→VK→YouTube, затем IG/TikTok) — по спросу.
- **Админка полная** (GPU/провайдеры/skill-модерация разделы) — следом за соответствующими подсистемами.
- **Своя GPU** (vLLM) — только при 500+ платящих; сейчас serverless DeepInfra.

## 📋 Мелочи
- Расход токенов в профиле — блок есть, наполнится при metering на своей модели.
- Дозаполнить оставшиеся тонкие подотделы агентами по мере спроса.
- Reality Checker + legal-ревью OSINT/кибербез перед публичным релизом (тест-gate «отказ от пробива/несанкционированного»).
